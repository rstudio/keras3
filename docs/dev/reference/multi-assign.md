# Assign values to names

See `%<-%` for more details.

## Usage

``` r
x %<-% value
```

## Value

The right-hand-side argument, `value`, invisibly. This called primarily
for its side-effect of assigning symbols in the current frame.
