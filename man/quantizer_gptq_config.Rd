% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantizers.R
\name{quantizer_gptq_config}
\alias{quantizer_gptq_config}
\title{GPTQ calibration and quantization configuration}
\usage{
quantizer_gptq_config(
  dataset,
  tokenizer,
  ...,
  weight_bits = 4L,
  num_samples = 128L,
  per_channel = TRUE,
  sequence_length = 512L,
  hessian_damping = 0.01,
  group_size = 128L,
  symmetric = FALSE,
  activation_order = FALSE,
  quantization_layer_structure = NULL
)
}
\arguments{
\item{dataset}{Calibration data used to analyze activation patterns. It can
be an iterable that yields strings or pre-tokenized numeric tensors, such
as a character vector, generator, R array, or NumPy array.}

\item{tokenizer}{Tokenizer or compatible callable used to process \code{dataset}
when it contains strings.}

\item{...}{For forward/backward compatibility. Arguments after \code{...} must be
named.}

\item{weight_bits}{Number of bits used for weight quantization. GPTQ supports
2, 3, 4, and 8. Defaults to 4.}

\item{num_samples}{Number of calibration samples to use. Defaults to 128.}

\item{per_channel}{Whether to calculate quantization parameters per output
channel. Defaults to \code{TRUE}.}

\item{sequence_length}{Sequence length of each calibration sample. Defaults
to 512.}

\item{hessian_damping}{Fraction of Hessian damping used to stabilize the
inverse calculation. Must be between 0 and 1. Defaults to 0.01.}

\item{group_size}{Number of weights quantized together. Use \code{-1} for
per-channel quantization. Defaults to 128.}

\item{symmetric}{Whether to use symmetric rather than asymmetric
quantization. Defaults to \code{FALSE}.}

\item{activation_order}{Whether to reorder weight columns by activation
magnitude, which can improve quantization accuracy. Defaults to \code{FALSE}.}

\item{quantization_layer_structure}{Optional named list describing
the model's quantization structure. It should contain
\code{pre_block_layers}, a list of layers to run before the first block, and
\code{sequential_blocks}, a list of transformer blocks to quantize
sequentially. If omitted, the model must provide
\code{get_quantization_layer_structure()}.}
}
\value{
A \code{GPTQConfig} instance.
}
\description{
GPTQ is a post-training method that quantizes weights to lower precision
while reducing the impact on model accuracy. It uses calibration data to
estimate a Hessian for each layer, applies iterative quantization with error
correction, optionally reorders weights by activation importance, and
minimizes quantization error. It can reduce model size and memory use and
does not require retraining a pretrained model.
}
\details{
Quantization quality depends heavily on the calibration dataset. For best
results, use representative data covering the expected input distribution.
}
\section{Examples}{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{config <- quantizer_gptq_config(
  dataset = calibration_text,
  tokenizer = tokenizer,
  weight_bits = 4,
  group_size = 128
)
model |> quantize_weights(mode = "gptq", config = config)
}\if{html}{\out{</div>}}
}

\references{
\itemize{
\item \href{https://arxiv.org/abs/2210.17323}{Frantar et al., 2022, \emph{GPTQ: Accurate Post-Training Quantization for Generative Pre-trained Transformers}}
\item \href{https://github.com/IST-DASLab/gptq}{Reference implementation}
}
}
\seealso{
Other quantizers: \cr
\code{\link{quantizer_abs_max_quantize_grouped_with_zero_point}()} \cr
\code{\link{quantizer_awq_config}()} \cr
\code{\link{quantizer_float8_quantization_config}()} \cr
\code{\link{quantizer_int4_quantization_config}()} \cr
\code{\link{quantizer_int8_quantization_config}()} \cr
\code{\link{quantizer_quantization_config}()} \cr
}
\concept{quantizers}
