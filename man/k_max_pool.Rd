% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops.R
\name{k_max_pool}
\alias{k_max_pool}
\title{Max pooling operation.}
\usage{
k_max_pool(
  inputs,
  pool_size,
  strides = NULL,
  padding = "valid",
  data_format = NULL
)
}
\arguments{
\item{inputs}{Tensor of rank N+2. \code{inputs} has shape
\verb{(batch_size,) + inputs_spatial_shape + (num_channels,)} if
\code{data_format="channels_last"}, or
\verb{(batch_size, num_channels) + inputs_spatial_shape} if
\code{data_format="channels_first"}. Pooling happens over the spatial
dimensions only.}

\item{pool_size}{int or tuple/list of integers of size
\code{len(inputs_spatial_shape)}, specifying the size of the pooling
window for each spatial dimension of the input tensor. If
\code{pool_size} is int, then every spatial dimension shares the same
\code{pool_size}.}

\item{strides}{int or tuple/list of integers of size
\code{len(inputs_spatial_shape)}. The stride of the sliding window for
each spatial dimension of the input tensor. If \code{strides} is int,
then every spatial dimension shares the same \code{strides}.}

\item{padding}{string, either \code{"valid"} or \code{"same"}. \code{"valid"} means no
padding is applied, and \code{"same"} results in padding evenly to the
left/right or up/down of the input such that output has the
same height/width dimension as the input when \code{strides=1}.}

\item{data_format}{A string, either \code{"channels_last"} or \code{"channels_first"}.
\code{data_format} determines the ordering of the dimensions in the
inputs. If \code{data_format="channels_last"}, \code{inputs} is of shape
\verb{(batch_size, ..., channels)} while if
\code{data_format="channels_first"}, \code{inputs} is of shape
\verb{(batch_size, channels, ...)}.}
}
\description{
Max pooling operation.
}
\section{Returns}{
\if{html}{\out{<div class="sourceCode">}}\preformatted{A tensor of rank N+2, the result of the max pooling operation.
}\if{html}{\out{</div>}}
}

\seealso{
\itemize{
\item \url{https://www.tensorflow.org/api_docs/python/tf/keras/ops/max_pool}
}
}
