% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops.R
\name{k_depthwise_conv}
\alias{k_depthwise_conv}
\title{General N-D depthwise convolution.}
\usage{
k_depthwise_conv(
  inputs,
  kernel,
  strides = 1L,
  padding = "valid",
  data_format = NULL,
  dilation_rate = 1L
)
}
\arguments{
\item{inputs}{Tensor of rank N+2. \code{inputs} has shape
\verb{(batch_size,)} + inputs_spatial_shape + (num_channels,)\code{if}data_format="channels_last"\verb{, or }(batch_size, num_channels) + inputs_spatial_shape\code{if}data_format="channels_first"`.}

\item{kernel}{Tensor of rank N+2. \code{kernel} has shape
\link{kernel_spatial_shape, num_input_channels, num_channels_multiplier},
\code{num_input_channels} should match the number of channels in
\code{inputs}.}

\item{strides}{int or int tuple/list of \code{len(inputs_spatial_shape)},
specifying the strides of the convolution along each spatial
dimension. If \code{strides} is int, then every spatial dimension shares
the same \code{strides}.}

\item{padding}{string, either \code{"valid"} or \code{"same"}. \code{"valid"} means no
padding is applied, and \code{"same"} results in padding evenly to the
left/right or up/down of the input such that output has the
same height/width dimension as the input when \code{strides=1}.}

\item{data_format}{A string, either \code{"channels_last"} or \code{"channels_first"}.
\code{data_format} determines the ordering of the dimensions in the
inputs. If \code{data_format="channels_last"}, \code{inputs} is of shape
\verb{(batch_size, ..., channels)} while if
\code{data_format="channels_first"}, \code{inputs} is of shape
\verb{(batch_size, channels, ...)}.}

\item{dilation_rate}{int or int tuple/list of \code{len(inputs_spatial_shape)},
specifying the dilation rate to use for dilated convolution. If
\code{dilation_rate} is int, then every spatial dimension shares
the same \code{dilation_rate}.}
}
\description{
This ops supports 1D and 2D depthwise convolution.
}
\section{Returns}{
\if{html}{\out{<div class="sourceCode">}}\preformatted{A tensor of rank N+2, the result of the depthwise conv operation.
}\if{html}{\out{</div>}}
}

\seealso{
\itemize{
\item \url{https://www.tensorflow.org/api_docs/python/tf/keras/ops/depthwise_conv}
}
}
