% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops.R
\name{k_average}
\alias{k_average}
\title{Compute the weighted average along the specified axis.}
\usage{
k_average(x, axis = NULL, weights = NULL)
}
\arguments{
\item{x}{Input tensor.}

\item{axis}{Integer along which to average \code{x}. The default, \code{axis=None},
will average over all of the elements of the input tensor. If axis
is negative it counts from the last to the first axis.}

\item{weights}{Tensor of wieghts associated with the values in \code{x}. Each
value in \code{x} contributes to the average according to its
associated weight. The weights array can either be 1-D (in which
case its length must be the size of a along the given axis) or of
the same shape as \code{x}. If \code{weights=None} (default), then all data
in \code{x} are assumed to have a weight equal to one.

\if{html}{\out{<div class="sourceCode">}}\preformatted{The 1-D calculation is: `avg = sum(a * weights) / sum(weights)`.
The only constraint on weights is that `sum(weights)` must not be 0.
}\if{html}{\out{</div>}}}
}
\description{
Compute the weighted average along the specified axis.
}
\section{Returns}{
Return the average along the specified axis.
}

\section{Examples}{
\if{html}{\out{<div class="sourceCode python">}}\preformatted{data = keras_core.ops.arange(1, 5)
data
# array([1, 2, 3, 4], dtype=int32)
keras_core.ops.average(data)
# array(2.5, dtype=float32)
keras_core.ops.average(
    keras_core.ops.arange(1, 11),
    weights=keras_core.ops.arange(10, 0, -1)
)
# array(4., dtype=float32)
}\if{html}{\out{</div>}}

\if{html}{\out{<div class="sourceCode python">}}\preformatted{data = keras_core.ops.arange(6).reshape((3, 2))
data
# array([[0, 1],
#        [2, 3],
#        [4, 5]], dtype=int32)
keras_core.ops.average(
    data,
    axis=1,
    weights=keras_core.ops.array([1./4, 3./4])
)
# array([0.75, 2.75, 4.75], dtype=float32)
keras_core.ops.average(
    data,
    weights=keras_core.ops.array([1./4, 3./4])
)
# Traceback (most recent call last):
#     ...
# ValueError: Axis must be specified when shapes of a and weights differ.
}\if{html}{\out{</div>}}
}

\seealso{
\itemize{
\item \url{https://www.tensorflow.org/api_docs/python/tf/keras/ops/average}
}
}
