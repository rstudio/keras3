% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops.R
\name{k_diagonal}
\alias{k_diagonal}
\title{Return specified diagonals.}
\usage{
k_diagonal(x, offset = 0L, axis1 = 0L, axis2 = 1L)
}
\arguments{
\item{x}{Input tensor.}

\item{offset}{Offset of the diagonal from the main diagonal.
Can be positive or negative. Defaults to \code{0}.(main diagonal).}

\item{axis1}{Axis to be used as the first axis of the 2-D sub-arrays.
Defaults to \code{0}.(first axis).}

\item{axis2}{Axis to be used as the second axis of the 2-D sub-arrays.
Defaults to \code{1} (second axis).}
}
\description{
If \code{x} is 2-D, returns the diagonal of \code{x} with the given offset, i.e., the
collection of elements of the form \code{x[i, i+offset]}.

If \code{x} has more than two dimensions, the axes specified by \code{axis1}
and \code{axis2} are used to determine the 2-D sub-array whose diagonal
is returned.

The shape of the resulting array can be determined by removing \code{axis1}
and \code{axis2} and appending an index to the right equal to the size of
the resulting diagonals.
}
\section{Returns}{
Tensor of diagonals.
}

\section{Examples}{
\if{html}{\out{<div class="sourceCode python">}}\preformatted{from keras_core import ops
x = ops.arange(4).reshape((2, 2))
x
# array([[0, 1],
#        [2, 3]])
x.diagonal()
# array([0, 3])
x.diagonal(1)
# array([1])
}\if{html}{\out{</div>}}

\if{html}{\out{<div class="sourceCode python">}}\preformatted{x = ops.arange(8).reshape((2, 2, 2))
x
# array([[[0, 1],
#         [2, 3]],
#        [[4, 5],
#         [6, 7]]])
x.diagonal(0, 0, 1)
# array([[0, 6],
#        [1, 7]])
}\if{html}{\out{</div>}}
}

\seealso{
\itemize{
\item \url{https://www.tensorflow.org/api_docs/python/tf/keras/ops/diagonal}
}
}
