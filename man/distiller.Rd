% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distillation.R
\name{distiller}
\alias{distiller}
\title{Model for transferring knowledge from a teacher to a student}
\usage{
distiller(
  teacher,
  student,
  distillation_losses,
  distillation_loss_weights = NULL,
  student_loss_weight = 0.5,
  name = "distiller",
  ...
)
}
\arguments{
\item{teacher}{Trained Keras model that provides the knowledge to transfer.
The teacher is frozen by the distiller.}

\item{student}{Keras model to train.}

\item{distillation_losses}{A distillation loss or list of distillation
losses, such as \code{\link[=distillation_logits]{distillation_logits()}},
\code{\link[=distillation_feature]{distillation_feature()}}, or compatible upstream distillation losses.}

\item{distillation_loss_weights}{Numeric vector of weights for the
distillation losses. It must have the same length as
\code{distillation_losses}. If \code{NULL}, equal weights are used.}

\item{student_loss_weight}{Weight of the student's supervised loss. Must be
between 0 and 1. Defaults to 0.5.}

\item{name}{Name of the distiller model. Defaults to \code{"distiller"}.}

\item{...}{Additional arguments passed to the parent Keras \code{Model} class.}
}
\value{
A Keras \code{Distiller} model.
}
\description{
A distiller trains a student model from both ground-truth labels and the
predictions or intermediate features of a frozen teacher model. After
training, access \code{model$student} to use the trained student independently.
}
\section{Examples}{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{teacher <- keras_model_sequential(input_shape = 4) |>
  layer_dense(8, activation = "relu") |>
  layer_dense(3)
student <- keras_model_sequential(input_shape = 4) |>
  layer_dense(3)

model <- distiller(
  teacher = teacher,
  student = student,
  distillation_losses = distillation_logits(temperature = 3)
)
model |> compile(optimizer = "adam", loss = "mse")
}\if{html}{\out{</div>}}
}

\seealso{
Other distillation: \cr
\code{\link{distillation_feature}()} \cr
\code{\link{distillation_logits}()} \cr

Other model creation: \cr
\code{\link{keras_input}()} \cr
\code{\link{keras_model}()} \cr
\code{\link{keras_model_sequential}()} \cr
}
\concept{distillation}
\concept{model creation}
