% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gen-layers-merging.R, R/layers-merging.R
\name{layer_dot}
\alias{layer_dot}
\title{Computes element-wise dot product of two tensors.}
\usage{
layer_dot(inputs, ..., axes, normalize = FALSE)

layer_dot(inputs, ..., axes, normalize = FALSE)
}
\arguments{
\item{...}{Standard layer keyword arguments.}

\item{axes}{Integer or tuple of integers, axis or axes along which to
take the dot product. If a tuple, should be two integers
corresponding to the desired axis from the first input and the
desired axis from the second input, respectively. Note that the
size of the two selected axes must match.}

\item{normalize}{Whether to L2-normalize samples along the dot product axis
before taking the dot product. If set to \code{True}, then
the output of the dot product is the cosine proximity
between the two samples.}
}
\description{
It takes a list of inputs of size 2, and the axes
corresponding to each input along with the dot product
is to be performed.

Let's say \code{x} and \code{y} are the two input tensors with shapes
\verb{(2, 3, 5)} and \verb{(2, 10, 3)}. The batch dimension should be
of same size for both the inputs, and \code{axes} should correspond
to the dimensions that have the same size in the corresponding
inputs. e.g. with \verb{axes=(1, 2)}, the dot product of \code{x}, and \code{y}
will result in a tensor with shape \verb{(2, 5, 10)}

It takes a list of inputs of size 2, and the axes
corresponding to each input along with the dot product
is to be performed.

Let's say \code{x} and \code{y} are the two input tensors with shapes
\verb{(2, 3, 5)} and \verb{(2, 10, 3)}. The batch dimension should be
of same size for both the inputs, and \code{axes} should correspond
to the dimensions that have the same size in the corresponding
inputs. e.g. with \verb{axes=(1, 2)}, the dot product of \code{x}, and \code{y}
will result in a tensor with shape \verb{(2, 5, 10)}
}
\section{Examples}{
\if{html}{\out{<div class="sourceCode python">}}\preformatted{x = np.arange(10).reshape(1, 5, 2)
y = np.arange(10, 20).reshape(1, 2, 5)
keras_core.layers.Dot(axes=(1, 2))([x, y])
}\if{html}{\out{</div>}}

Usage in a Keras model:

\if{html}{\out{<div class="sourceCode python">}}\preformatted{x1 = keras_core.layers.Dense(8)(np.arange(10).reshape(5, 2))
x2 = keras_core.layers.Dense(8)(np.arange(10, 20).reshape(5, 2))
y = keras_core.layers.Dot(axes=1)([x1, x2])
}\if{html}{\out{</div>}}

\if{html}{\out{<div class="sourceCode python">}}\preformatted{x = np.arange(10).reshape(1, 5, 2)
y = np.arange(10, 20).reshape(1, 2, 5)
keras_core.layers.Dot(axes=(1, 2))([x, y])
}\if{html}{\out{</div>}}

Usage in a Keras model:

\if{html}{\out{<div class="sourceCode python">}}\preformatted{x1 = keras_core.layers.Dense(8)(np.arange(10).reshape(5, 2))
x2 = keras_core.layers.Dense(8)(np.arange(10, 20).reshape(5, 2))
y = keras_core.layers.Dot(axes=1)([x1, x2])
}\if{html}{\out{</div>}}
}

\section{Returns}{
\if{html}{\out{<div class="sourceCode">}}\preformatted{A tensor, the dot product of the samples from the inputs.
}\if{html}{\out{</div>}}

\if{html}{\out{<div class="sourceCode">}}\preformatted{A tensor, the dot product of the samples from the inputs.
}\if{html}{\out{</div>}}
}

\seealso{
\itemize{
\item \url{https://www.tensorflow.org/api_docs/python/tf/keras/layers/Dot}
}

\itemize{
\item \url{https://www.tensorflow.org/api_docs/python/tf/keras/layers/Dot}
}

Other merging layers: 
\code{\link{layer_add}()},
\code{\link{layer_average}()},
\code{\link{layer_concatenate}()},
\code{\link{layer_maximum}()},
\code{\link{layer_minimum}()},
\code{\link{layer_multiply}()},
\code{\link{layer_subtract}()}

Other merging layers: 
\code{\link{layer_add}()},
\code{\link{layer_average}()},
\code{\link{layer_concatenate}()},
\code{\link{layer_maximum}()},
\code{\link{layer_minimum}()},
\code{\link{layer_multiply}()},
\code{\link{layer_subtract}()}
}
\concept{merging layers}
