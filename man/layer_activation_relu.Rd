% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autogen-layers-activations.R,
%   R/layers-activations.R
\name{layer_activation_relu}
\alias{layer_activation_relu}
\title{Rectified Linear Unit activation function layer.}
\usage{
layer_activation_relu(
  object,
  max_value = NULL,
  negative_slope = 0,
  threshold = 0,
  ...
)

layer_activation_relu(
  object,
  max_value = NULL,
  negative_slope = 0,
  threshold = 0,
  ...
)
}
\arguments{
\item{max_value}{Float >= 0. Maximum activation value. None means unlimited.
Defaults to \code{None}.}

\item{negative_slope}{Float >= 0. Negative slope coefficient.
Defaults to \code{0.0}.}

\item{threshold}{Float >= 0. Threshold value for thresholded activation.
Defaults to \code{0.0}.}

\item{...}{Base layer keyword arguments, such as \code{name} and \code{dtype}.}
}
\description{
Formula:

\if{html}{\out{<div class="sourceCode python">}}\preformatted{f(x) = max(x,0)
f(x) = max_value if x >= max_value
f(x) = x if threshold <= x < max_value
f(x) = negative_slope * (x - threshold) otherwise
}\if{html}{\out{</div>}}

Formula:

\if{html}{\out{<div class="sourceCode python">}}\preformatted{f(x) = max(x,0)
f(x) = max_value if x >= max_value
f(x) = x if threshold <= x < max_value
f(x) = negative_slope * (x - threshold) otherwise
}\if{html}{\out{</div>}}
}
\section{Examples}{
\if{html}{\out{<div class="sourceCode python">}}\preformatted{relu_layer = keras_core.layers.activations.ReLU(
    max_value=10,
    negative_slope=0.5,
    threshold=0,
)
input = np.array([-10, -5, 0.0, 5, 10])
result = relu_layer(input)
# result = [-5. , -2.5,  0. ,  5. , 10.]
}\if{html}{\out{</div>}}

\if{html}{\out{<div class="sourceCode python">}}\preformatted{relu_layer = keras_core.layers.activations.ReLU(
    max_value=10,
    negative_slope=0.5,
    threshold=0,
)
input = np.array([-10, -5, 0.0, 5, 10])
result = relu_layer(input)
# result = [-5. , -2.5,  0. ,  5. , 10.]
}\if{html}{\out{</div>}}
}

\seealso{
\itemize{
\item \url{https://www.tensorflow.org/api_docs/python/tf/keras/layers/ReLU}
}

\itemize{
\item \url{https://www.tensorflow.org/api_docs/python/tf/keras/layers/ReLU}
}

Other activations layers: 
\code{\link{layer_activation_elu}()},
\code{\link{layer_activation_leaky_relu}()},
\code{\link{layer_activation_parametric_relu}()},
\code{\link{layer_activation_softmax}()},
\code{\link{layer_activation}()}

Other activations layers: 
\code{\link{layer_activation_elu}()},
\code{\link{layer_activation_leaky_relu}()},
\code{\link{layer_activation_parametric_relu}()},
\code{\link{layer_activation_softmax}()},
\code{\link{layer_activation}()}
}
\concept{activations layers}
