% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops.R
\name{k_linspace}
\alias{k_linspace}
\title{Return evenly spaced numbers over a specified interval.}
\usage{
k_linspace(
  start,
  stop,
  num = 50L,
  endpoint = TRUE,
  retstep = FALSE,
  dtype = NULL,
  axis = 0L
)
}
\arguments{
\item{start}{The starting value of the sequence.
stop: The end value of the sequence, unless \code{endpoint} is set to
\code{False}. In that case, the sequence consists of all but the last
of \code{num + 1} evenly spaced samples, so that \code{stop} is excluded.
Note that the step size changes when \code{endpoint} is \code{False}.
num: Number of samples to generate. Defaults to \code{50}. Must be
non-negative.
endpoint: If \code{True}, \code{stop} is the last sample. Otherwise, it is
not included. Defaults to\code{True}.
retstep: If \code{True}, return \verb{(samples, step)}, where \code{step} is the
spacing between samples.
dtype: The type of the output tensor.
axis: The axis in the result to store the samples. Relevant only if
start or stop are array-like. Defaults to \code{0}.

Note:
Torch backend does not support \code{axis} argument.}
}
\description{
Returns \code{num} evenly spaced samples, calculated over the interval
\verb{[start, stop]}.

The endpoint of the interval can optionally be excluded.
}
\section{Returns}{
A tensor of evenly spaced numbers.
If \code{retstep} is \code{True}, returns \verb{(samples, step)}
}

\seealso{
\itemize{
\item \url{https://www.tensorflow.org/api_docs/python/tf/keras/ops/linspace}
}
}
