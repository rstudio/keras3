% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops.R
\name{k_dot}
\alias{k_dot}
\title{Dot product of two tensors.}
\usage{
k_dot(x1, x2)
}
\arguments{
\item{x1}{First argument.
x2: Second argument.

Note:
Torch backend does not accept 0-D tensors as arguments.}
}
\description{
\itemize{
\item If both \code{x1} and \code{x2} are 1-D tensors, it is inner product of vectors
(without complex conjugation).
\item If both \code{x1} and \code{x2} are 2-D tensors, it is matrix multiplication.
\item If either \code{x1} or \code{x2} is 0-D (scalar), it is equivalent to \code{x1 * x2}.
\item If \code{x1} is an N-D tensor and \code{x2} is a 1-D tensor, it is a sum product
over the last axis of \code{x1} and \code{x2}.
\item If \code{x1} is an N-D tensor and \code{x2} is an M-D tensor (where \code{M>=2}),
it is a sum product over the last axis of \code{x1} and the second-to-last
axis of \code{x2}: \verb{dot(x1, x2)[i,j,k,m] = sum(a[i,j,:] * b[k,:,m])}.
}
}
\section{Returns}{
\if{html}{\out{<div class="sourceCode">}}\preformatted{Dot product of `x1` and `x2`.
}\if{html}{\out{</div>}}
}

\seealso{
\itemize{
\item \url{https://www.tensorflow.org/api_docs/python/tf/keras/ops/dot}
}
}
