% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pooling.R
\name{layer_average_pooling_2d}
\alias{layer_average_pooling_2d}
\title{Average pooling operation for spatial data.}
\usage{
layer_average_pooling_2d(x, pool_size = c(2L, 2L), strides = NULL,
  padding = "valid", data_format = NULL)
}
\arguments{
\item{x}{Model or layer}

\item{pool_size}{integer or list of 2 integers, factors by which to downscale
(vertical, horizontal). (2, 2) will halve the input in both spatial
dimension. If only one integer is specified, the same window length will be
used for both dimensions.}

\item{strides}{Integer, list of 2 integers, or NULL. Strides values. If NULL,
it will default to \code{pool_size}.}

\item{padding}{One of \code{"valid"} or \code{"same"} (case-insensitive).}

\item{data_format}{A string, one of \code{channels_last} (default) or
\code{channels_first}. The ordering of the dimensions in the inputs.
\code{channels_last} corresponds to inputs with shape \code{(batch, width, height, channels)} while \code{channels_first} corresponds to inputs with shape \code{(batch, channels, width, height)}. It defaults to the \code{image_data_format} value
found in your Keras config file at \code{~/.keras/keras.json}. If you never set
it, then it will be "channels_last".}
}
\description{
Average pooling operation for spatial data.
}
\section{Input shape}{

\itemize{
\item If \code{data_format='channels_last'}: 4D tensor with shape: \code{(batch_size, rows, cols, channels)}
\item If \code{data_format='channels_first'}: 4D tensor with shape: \code{(batch_size, channels, rows, cols)}
}
}

\section{Output shape}{

\itemize{
\item If \code{data_format='channels_last'}: 4D tensor with shape: \code{(batch_size, pooled_rows, pooled_cols, channels)}
\item If \code{data_format='channels_first'}: 4D tensor with shape: \code{(batch_size, channels, pooled_rows, pooled_cols)}
}
}

