## Autogenerated. Do not modify manually.


# keras.config.backend
# keras.src.backend.config.backend
r"-(Publicly accessible method for determining the current backend.

    Returns:
        String, the name of the backend Keras is currently using. One of
            `"tensorflow"`, `"torch"`, or `"jax"`.

    Example:

    >>> keras.config.backend()
    'tensorflow'

    )-"
#' Publicly accessible method for determining the current backend.
#'
#' @description
#'
#' # Returns
#' String, the name of the backend Keras is currently using. One of
#' `"tensorflow"`, `"torch"`, or `"jax"`.
#'
#' # Examples
#' ```python
#' keras.config.backend()
#' # 'tensorflow'
#' ```
#'
#' @export
#' @seealso
#' + <https://www.tensorflow.org/api_docs/python/tf/keras/config/backend>
config_backend <-
function ()
{
    args <- capture_args2(NULL)
    do.call(keras$config$backend, args)
}


# keras.config.epsilon
# keras.src.backend.config.epsilon
r"-(Return the value of the fuzz factor used in numeric expressions.

    Returns:
        A float.

    Example:

    >>> keras.config.epsilon()
    1e-07

    )-"
#' Return the value of the fuzz factor used in numeric expressions.
#'
#' @description
#'
#' # Returns
#' A float.
#'
#' # Examples
#' ```python
#' keras.config.epsilon()
#' # 1e-07
#' ```
#'
#' @export
#' @seealso
#' + <https://www.tensorflow.org/api_docs/python/tf/keras/config/epsilon>
config_epsilon <-
function ()
{
    args <- capture_args2(NULL)
    do.call(keras$config$epsilon, args)
}


# keras.config.floatx
# keras.src.backend.config.floatx
r"-(Return the default float type, as a string.

    E.g. `'float16'`, `'float32'`, `'float64'`.

    Returns:
        String, the current default float type.

    Example:

    >>> keras.config.floatx()
    'float32'

    )-"
#' Return the default float type, as a string.
#'
#' @description
#' E.g. `'float16'`, `'float32'`, `'float64'`.
#'
#' # Returns
#' String, the current default float type.
#'
#' # Examples
#' ```python
#' keras.config.floatx()
#' # 'float32'
#' ```
#'
#' @export
#' @seealso
#' + <https://www.tensorflow.org/api_docs/python/tf/keras/config/floatx>
config_floatx <-
function ()
{
    args <- capture_args2(NULL)
    do.call(keras$config$floatx, args)
}


# keras.config.image_data_format
# keras.src.backend.config.image_data_format
r"-(Return the default image data format convention.

    Returns:
        A string, either `'channels_first'` or `'channels_last'`.

    Example:

    >>> keras.config.image_data_format()
    'channels_last'

    )-"
#' Return the default image data format convention.
#'
#' @description
#'
#' # Returns
#' A string, either `'channels_first'` or `'channels_last'`.
#'
#' # Examples
#' ```python
#' keras.config.image_data_format()
#' # 'channels_last'
#' ```
#'
#' @export
#' @seealso
#' + <https://www.tensorflow.org/api_docs/python/tf/keras/config/image_data_format>
config_image_data_format <-
function ()
{
    args <- capture_args2(NULL)
    do.call(keras$config$image_data_format, args)
}


# keras.config.set_epsilon
# keras.src.backend.config.set_epsilon
r"-(Set the value of the fuzz factor used in numeric expressions.

    Args:
        value: float. New value of epsilon.

    Examples:
    >>> keras.config.epsilon()
    1e-07

    >>> keras.config.set_epsilon(1e-5)
    >>> keras.config.epsilon()
    1e-05

    >>> # Set it back to the default value.
    >>> keras.config.set_epsilon(1e-7)

    )-"
#' Set the value of the fuzz factor used in numeric expressions.
#'
#' @description
#'
#' # Examples
#' ```python
#' keras.config.epsilon()
#' # 1e-07
#' ```
#'
#' ```python
#' keras.config.set_epsilon(1e-5)
#' keras.config.epsilon()
#' # 1e-05
#' ```
#'
#' ```python
#' # Set it back to the default value.
#' keras.config.set_epsilon(1e-7)
#' ```
#'
#' @param value float. New value of epsilon.
#'
#' @export
#' @seealso
#' + <https://www.tensorflow.org/api_docs/python/tf/keras/config/set_epsilon>
config_set_epsilon <-
function (value)
{
    args <- capture_args2(NULL)
    do.call(keras$config$set_epsilon, args)
}


# keras.config.set_floatx
# keras.src.backend.config.set_floatx
r"-(Set the default float dtype.

    Note: It is not recommended to set this to `"float16"` for training,
    as this will likely cause numeric stability issues.
    Instead, mixed precision, which leverages
    a mix of `float16` and `float32`. It can be configured by calling
    `keras.mixed_precision.set_dtype_policy('mixed_float16')`.

    Args:
        value: String; `'float16'`, `'float32'`, or `'float64'`.

    Examples:
    >>> keras.config.floatx()
    'float32'

    >>> keras.config.set_floatx('float64')
    >>> keras.config.floatx()
    'float64'

    >>> # Set it back to float32
    >>> keras.config.set_floatx('float32')

    Raises:
        ValueError: In case of invalid value.
    )-"
#' Set the default float dtype.
#'
#' @description
#'
#' # Note
#' It is not recommended to set this to `"float16"` for training,
#' as this will likely cause numeric stability issues.
#' Instead, mixed precision, which leverages
#' a mix of `float16` and `float32`. It can be configured by calling
#' `keras.mixed_precision.set_dtype_policy('mixed_float16')`.
#'
#' # Examples
#' ```python
#' keras.config.floatx()
#' # 'float32'
#' ```
#'
#' ```python
#' keras.config.set_floatx('float64')
#' keras.config.floatx()
#' # 'float64'
#' ```
#'
#' ```python
#' # Set it back to float32
#' keras.config.set_floatx('float32')
#' ```
#'
#' # Raises
#'     ValueError: In case of invalid value.
#'
#' @param value String; `'float16'`, `'float32'`, or `'float64'`.
#'
#' @export
#' @seealso
#' + <https://www.tensorflow.org/api_docs/python/tf/keras/config/set_floatx>
config_set_floatx <-
function (value)
{
    args <- capture_args2(NULL)
    do.call(keras$config$set_floatx, args)
}


# keras.config.set_image_data_format
# keras.src.backend.config.set_image_data_format
r"-(Set the value of the image data format convention.

    Args:
        data_format: string. `'channels_first'` or `'channels_last'`.

    Examples:

    >>> keras.config.image_data_format()
    'channels_last'

    >>> keras.config.set_image_data_format('channels_first')
    >>> keras.config.image_data_format()
    'channels_first'

    >>> # Set it back to `'channels_last'`
    >>> keras.config.set_image_data_format('channels_last')

    )-"
#' Set the value of the image data format convention.
#'
#' @description
#'
#' # Examples
#' ```python
#' keras.config.image_data_format()
#' # 'channels_last'
#' ```
#'
#' ```python
#' keras.config.set_image_data_format('channels_first')
#' keras.config.image_data_format()
#' # 'channels_first'
#' ```
#'
#' ```python
#' # Set it back to `'channels_last'`
#' keras.config.set_image_data_format('channels_last')
#' ```
#'
#' @param data_format string. `'channels_first'` or `'channels_last'`.
#'
#' @export
#' @seealso
#' + <https://www.tensorflow.org/api_docs/python/tf/keras/config/set_image_data_format>
config_set_image_data_format <-
function (data_format)
{
    args <- capture_args2(NULL)
    do.call(keras$config$set_image_data_format, args)
}


# keras.config.enable_unsafe_deserialization
# keras.src.saving.serialization_lib.enable_unsafe_deserialization
r"-(Disables safe mode globally, allowing deserialization of lambdas.)-"
#' Disables safe mode globally, allowing deserialization of lambdas.
#'
#' @export
#' @seealso
#' + <https://www.tensorflow.org/api_docs/python/tf/keras/config/enable_unsafe_deserialization>
config_enable_unsafe_deserialization <-
function ()
{
    args <- capture_args2(NULL)
    do.call(keras$config$enable_unsafe_deserialization, args)
}


# keras.config.disable_interactive_logging
# keras.src.utils.io_utils.disable_interactive_logging
r"-(Turn off interactive logging.

    When interactive logging is disabled, Keras sends logs to `absl.logging`.
    This is the best option when using Keras in a non-interactive
    way, such as running a training or inference job on a server.
    )-"
#' Turn off interactive logging.
#'
#' @description
#' When interactive logging is disabled, Keras sends logs to `absl.logging`.
#' This is the best option when using Keras in a non-interactive
#' way, such as running a training or inference job on a server.
#'
#' @export
#' @seealso
#' + <https://www.tensorflow.org/api_docs/python/tf/keras/config/disable_interactive_logging>
config_disable_interactive_logging <-
function ()
{
    args <- capture_args2(NULL)
    do.call(keras$config$disable_interactive_logging, args)
}


# keras.config.enable_interactive_logging
# keras.src.utils.io_utils.enable_interactive_logging
r"-(Turn on interactive logging.

    When interactive logging is enabled, Keras displays logs via stdout.
    This provides the best experience when using Keras in an interactive
    environment such as a shell or a notebook.
    )-"
#' Turn on interactive logging.
#'
#' @description
#' When interactive logging is enabled, Keras displays logs via stdout.
#' This provides the best experience when using Keras in an interactive
#' environment such as a shell or a notebook.
#'
#' @export
#' @seealso
#' + <https://www.tensorflow.org/api_docs/python/tf/keras/config/enable_interactive_logging>
config_enable_interactive_logging <-
function ()
{
    args <- capture_args2(NULL)
    do.call(keras$config$enable_interactive_logging, args)
}


# keras.config.is_interactive_logging_enabled
# keras.src.utils.io_utils.is_interactive_logging_enabled
r"-(Check if interactive logging is enabled.

    To switch between writing logs to stdout and `absl.logging`, you may use
    `keras.config.enable_interactive_logging()` and
    `keras.config.disable_interactie_logging()`.

    Returns:
        Boolean, `True` if interactive logging is enabled,
        and `False` otherwise.
    )-"
#' Check if interactive logging is enabled.
#'
#' @description
#' To switch between writing logs to stdout and `absl.logging`, you may use
#' `keras.config.enable_interactive_logging()` and
#' `keras.config.disable_interactie_logging()`.
#'
#' # Returns
#' Boolean, `True` if interactive logging is enabled,
#' and `False` otherwise.
#'
#' @export
#' @seealso
#' + <https://www.tensorflow.org/api_docs/python/tf/keras/config/is_interactive_logging_enabled>
config_is_interactive_logging_enabled <-
function ()
{
    args <- capture_args2(NULL)
    do.call(keras$config$is_interactive_logging_enabled, args)
}


# keras.config.disable_traceback_filtering
# keras.src.utils.traceback_utils.disable_traceback_filtering
r"-(Turn off traceback filtering.

    Raw Keras tracebacks (also known as stack traces)
    involve many internal frames, which can be
    challenging to read through, while not being actionable for end users.
    By default, Keras filters internal frames in most exceptions that it
    raises, to keep traceback short, readable, and focused on what's
    actionable for you (your own code).

    See also `keras.config.enable_traceback_filtering()` and
    `keras.config.is_traceback_filtering_enabled()`.

    If you have previously disabled traceback filtering via
    `keras.config.disable_traceback_filtering()`, you can re-enable it via
    `keras.config.enable_traceback_filtering()`.
    )-"
#' Turn off traceback filtering.
#'
#' @description
#' Raw Keras tracebacks (also known as stack traces)
#' involve many internal frames, which can be
#' challenging to read through, while not being actionable for end users.
#' By default, Keras filters internal frames in most exceptions that it
#' raises, to keep traceback short, readable, and focused on what's
#' actionable for you (your own code).
#'
#' See also `keras.config.enable_traceback_filtering()` and
#' `keras.config.is_traceback_filtering_enabled()`.
#'
#' If you have previously disabled traceback filtering via
#' `keras.config.disable_traceback_filtering()`, you can re-enable it via
#' `keras.config.enable_traceback_filtering()`.
#'
#' @export
#' @seealso
#' + <https://www.tensorflow.org/api_docs/python/tf/keras/config/disable_traceback_filtering>
config_disable_traceback_filtering <-
function ()
{
    args <- capture_args2(NULL)
    do.call(keras$config$disable_traceback_filtering, args)
}


# keras.config.enable_traceback_filtering
# keras.src.utils.traceback_utils.enable_traceback_filtering
r"-(Turn on traceback filtering.

    Raw Keras tracebacks (also known as stack traces)
    involve many internal frames, which can be
    challenging to read through, while not being actionable for end users.
    By default, Keras filters internal frames in most exceptions that it
    raises, to keep traceback short, readable, and focused on what's
    actionable for you (your own code).

    See also `keras.config.disable_traceback_filtering()` and
    `keras.config.is_traceback_filtering_enabled()`.

    If you have previously disabled traceback filtering via
    `keras.config.disable_traceback_filtering()`, you can re-enable it via
    `keras.config.enable_traceback_filtering()`.
    )-"
#' Turn on traceback filtering.
#'
#' @description
#' Raw Keras tracebacks (also known as stack traces)
#' involve many internal frames, which can be
#' challenging to read through, while not being actionable for end users.
#' By default, Keras filters internal frames in most exceptions that it
#' raises, to keep traceback short, readable, and focused on what's
#' actionable for you (your own code).
#'
#' See also `keras.config.disable_traceback_filtering()` and
#' `keras.config.is_traceback_filtering_enabled()`.
#'
#' If you have previously disabled traceback filtering via
#' `keras.config.disable_traceback_filtering()`, you can re-enable it via
#' `keras.config.enable_traceback_filtering()`.
#'
#' @export
#' @seealso
#' + <https://www.tensorflow.org/api_docs/python/tf/keras/config/enable_traceback_filtering>
config_enable_traceback_filtering <-
function ()
{
    args <- capture_args2(NULL)
    do.call(keras$config$enable_traceback_filtering, args)
}


# keras.config.is_traceback_filtering_enabled
# keras.src.utils.traceback_utils.is_traceback_filtering_enabled
r"-(Check if traceback filtering is enabled.

    Raw Keras tracebacks (also known as stack traces)
    involve many internal frames, which can be
    challenging to read through, while not being actionable for end users.
    By default, Keras filters internal frames in most exceptions that it
    raises, to keep traceback short, readable, and focused on what's
    actionable for you (your own code).

    See also `keras.config.enable_traceback_filtering()` and
    `keras.config.disable_traceback_filtering()`.

    If you have previously disabled traceback filtering via
    `keras.config.disable_traceback_filtering()`, you can re-enable it via
    `keras.config.enable_traceback_filtering()`.

    Returns:
        Boolean, `True` if traceback filtering is enabled,
        and `False` otherwise.
    )-"
#' Check if traceback filtering is enabled.
#'
#' @description
#' Raw Keras tracebacks (also known as stack traces)
#' involve many internal frames, which can be
#' challenging to read through, while not being actionable for end users.
#' By default, Keras filters internal frames in most exceptions that it
#' raises, to keep traceback short, readable, and focused on what's
#' actionable for you (your own code).
#'
#' See also `keras.config.enable_traceback_filtering()` and
#' `keras.config.disable_traceback_filtering()`.
#'
#' If you have previously disabled traceback filtering via
#' `keras.config.disable_traceback_filtering()`, you can re-enable it via
#' `keras.config.enable_traceback_filtering()`.
#'
#' # Returns
#' Boolean, `True` if traceback filtering is enabled,
#' and `False` otherwise.
#'
#' @export
#' @seealso
#' + <https://www.tensorflow.org/api_docs/python/tf/keras/config/is_traceback_filtering_enabled>
config_is_traceback_filtering_enabled <-
function ()
{
    args <- capture_args2(NULL)
    do.call(keras$config$is_traceback_filtering_enabled, args)
}
