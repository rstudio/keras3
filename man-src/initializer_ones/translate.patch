diff --git a/man-src/initializer_ones/2-translated.Rmd b/man-src/initializer_ones/2-translated.Rmd
--- a/man-src/initializer_ones/2-translated.Rmd
+++ b/man-src/initializer_ones/2-translated.Rmd
@@ -6,12 +6,12 @@ Also available via the shortcut function `ones`.
 # Examples
-```python
+```{r}
 # Standalone usage:
-initializer = Ones()
-values = initializer(shape=(2, 2))
+initializer <- initializer_ones()
+values <- initializer(shape = c(2, 2))
 ```
 
-```python
+```{r}
 # Usage in a Keras layer:
-initializer = Ones()
-layer = Dense(3, kernel_initializer=initializer)
+initializer <- initializer_ones()
+layer <- layer_dense(units = 3, kernel_initializer = initializer)
 ```
