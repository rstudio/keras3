diff --git a/man-src/initializer_orthogonal/2-translated.Rmd b/man-src/initializer_orthogonal/2-translated.Rmd
--- a/man-src/initializer_orthogonal/2-translated.Rmd
+++ b/man-src/initializer_orthogonal/2-translated.Rmd
@@ -10,3 +10,3 @@ Otherwise, the output will have orthogonal columns.
 If the shape of the tensor to initialize is more than two-dimensional,
-a matrix of shape `(shape[0] * ... * shape[n - 2], shape[n - 1])`
+a matrix of shape `(shape[1] * ... * shape[n - 1], shape[n])`
 is initialized, where `n` is the length of the shape vector.
@@ -15,12 +15,12 @@ The matrix is subsequently reshaped to give a tensor of the desired shape.
 # Examples
-```python
+```{r}
 # Standalone usage:
-initializer = keras.initializers.Orthogonal()
-values = initializer(shape=(2, 2))
+initializer <- initializer_orthogonal()
+values <- initializer(shape = c(2, 2))
 ```
 
-```python
+```{r}
 # Usage in a Keras layer:
-initializer = keras.initializers.Orthogonal()
-layer = keras.layers.Dense(3, kernel_initializer=initializer)
+initializer <- initializer_orthogonal()
+layer <- layer_dense(3, kernel_initializer = initializer)
 ```
@@ -31,3 +31,3 @@ layer = keras.layers.Dense(3, kernel_initializer=initializer)
 @param gain Multiplicative factor to apply to the orthogonal matrix.
-@param seed A Python integer. Used to make the behavior of the initializer
+@param seed An integer. Used to make the behavior of the initializer
     deterministic.
